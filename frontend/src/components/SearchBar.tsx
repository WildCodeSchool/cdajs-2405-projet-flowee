export default function SearchBar() {
  return (
    <div className="bg-black">
      <input
        type="text"
        placeholder="Looking for someting unique?"
        className="p-2 border bg-red-800 border-gray-300 rounded"
      />
    </div>
  );
}
